@model IEnumerable<Concordia_Web_App.Models.Class_Grade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grade_Percentage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Passed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Failed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grade_Percentage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Passed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Failed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
