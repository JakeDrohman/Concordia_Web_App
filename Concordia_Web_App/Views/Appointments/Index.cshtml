@model IEnumerable<Concordia_Web_App.Models.Appointment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Advisor.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.First_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reason_For_Appointment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Advisor.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.First_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reason_For_Appointment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
